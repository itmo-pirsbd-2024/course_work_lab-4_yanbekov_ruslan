syntax = "proto3";

option java_package = "com.example.imageprocessing";
option java_outer_classname = "ImageProcessingProto";

service ImageProcessingService {
  rpc processImage(ImageRequest) returns (ImageResponse);
}

enum OperationType {
  UNKNOWN = 0;
  RESIZE = 1;
  ROTATE = 2;
  GRAYSCALE = 3;
  INVERT = 4;
}

message ResizeParams {
  int32 width = 1;
  int32 height = 2;
}

message RotateParams {
  double angle = 1;
}

message ImageRequest {
  OperationType operation = 1;
  bytes imageData = 2;

  oneof operation_params {
    ResizeParams resizeParams = 3;
    RotateParams rotateParams = 4;
  }
}

message ImageResponse {
  bytes imageData = 1;
}
